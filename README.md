# 4gChat — AI Chatbot CLI

This is a AI chatbot CLI to be used in the terminal to help automate running commands. 

## Sample Usecase

```bash
4gChat create a file nums.txt in this current directory
```

This should run the command 
```bash
touch nums.txt
```